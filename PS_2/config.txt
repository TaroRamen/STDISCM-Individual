# Config file

# n - maximum number of concurrent instances
n = 64

# t - number of tank players in the queue
t = 200

# h - number of healer players in the queue
h = 200

# d - number of DPS players in the queue
d = 1000

# t1 - minimum time before an instance is finished
t1 = 2

# t2 - maximum time before an instance is finished 
t2 = 5

# bonus - activate bonus or not (true/false)
bonus = true

# t3 - minimum time before thread creates more players
t3 = 1

# t4 - maximum time before thread creates more players
t4 = 3

# t_max - max number of tank players that can be added every create
t_max = 100

# h_max - max number of healer players that can be added every create
h_max = 100

# d_max - max number of DPS players that can be added every create
d_max = 400